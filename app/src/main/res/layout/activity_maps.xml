<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.phunnylabs.assignmentloktra.activities.MapsActivity" />

    <ImageView
        android:id="@+id/imageViewShowPastTrips"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|left"
        android:layout_margin="30dp"
        android:adjustViewBounds="true"
        android:background="#80AAAAAA"
        android:padding="5dp"
        android:scaleType="centerInside"
        android:src="@drawable/map" />

    <ImageView
        android:id="@+id/imageViewStartEndTrip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center"
        android:layout_margin="20dp"
        android:src="@drawable/start" />

    <ListView
        android:id="@+id/listViewTrips"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white"
        android:clipToPadding="true"
        android:paddingTop="20dp"
        app:layout_behavior="android.support.design.widget.BottomSheetBehavior" />

    <TextView
        android:id="@+id/textViewTripTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center|top"
        android:layout_margin="10dp"
        android:background="@color/colorSecondary"
        android:padding="10dp"
        android:text="Time"
        android:textColor="@color/colorWhite"
        android:textSize="14sp" />

</android.support.design.widget.CoordinatorLayout>